import React from 'react';
import CustomLogin from '../components/CustomLogin'; // Adjust the path if necessary

const LoginPage = () => {
  return (
    <div>
      <CustomLogin />
    </div>
  );
};

export default LoginPage;